; Standalone office-door lock controller.
; Self-contained: no backend, no database, no app build. The ESP32 hosts
; its own login-protected web page on the office WiFi. See README.md.

[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200
